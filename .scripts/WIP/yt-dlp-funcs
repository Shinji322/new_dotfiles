function yt-dlp-ca {  
    if [ -z "$1" ]; then 
    # display intention if no parameters given
        echo "For channel archives"
    else
        for n in "$@"
        do 
            if [ -f "$n" ]
    fi
}
