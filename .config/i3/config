***REMOVED*** ~-|Keybinding Aliases!|-~
set $mod Mod4
set $alt Mod1
set $tab 0xff09

***REMOVED*** ~-|Startup Programs / Startup Scripts|-~
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec ~/.scripts/system/startup

***REMOVED*** Default information
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
font pango:monospace 10
***REMOVED*** Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


***REMOVED*** ~-|Gaps!!|-~ 
for_window [class=".*"] border pixel 3 
***REMOVED*** smart_gaps on 
gaps inner 5
gaps outer 5

***REMOVED*** change container layout (stacked, tabbed, toggle split)
***REMOVED***bindsym $mod+s layout stacking
***REMOVED***bindsym $mod+w layout tabbed
***REMOVED***bindsym $mod+e layout toggle split

***REMOVED*** toggle tiling / floating
bindsym $mod+Shift+space floating toggle

***REMOVED*** change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

***REMOVED*** focus the parent container
***REMOVED***bindsym $mod+a focus parent


***REMOVED*** restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
***REMOVED*** exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

***REMOVED*** Start i3bar to display a workspace bar (plus the system information i3status
***REMOVED*** finds out, if available)
bar {
    status_command i3blocks -c $XDG_CONFIG_HOME/i3blocks/i3blocks.conf
}
