#!/usr/bin/env bash

CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/scripts/tmux"
selected=`cat $CONFIG_DIR/cht.sh.languages $CONFIG_DIR/cht.sh.progs | fzf`

read -p "Enter Query: " query

if grep -qs "$selected" "$CONFIG_DIR/cht.sh.languages"; then
    query=`echo $query | tr ' ' '+'`
    bash -c "echo \"curl cht.sh/$selected/$query/\" & curl cht.sh/$selected/$query & while [ : ]; do sleep 1; done"
else
    bash -c "curl -s cht.sh/$selected~$query | less"
fi

