#!/usr/bin/env bash

MONITORS="$(xrandr -q | grep ' connected' | cut -d ' ' -f1)"
LEN="${#MONITORS[@]}"

dual_monitors() {
  xrandr --output eDP-1 --primary --mode 2560x1600 HDMI-A-1-0 --mode 1920x1080 --right-of eDP-1
}

if [ "$LEN" -gt 1 ]; then
  dual_monitors
fi
