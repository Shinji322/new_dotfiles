#!/bin/bash -x

MONITORS="$(xrandr -q | grep ' connected' | cut -d ' ' -f1)"
LEN=$(wc -l <<< "$MONITORS")

dual_monitors() {
  xrandr --output eDP-1 --mode 2560x1600 \
    --output HDMI-A-1-0 --primary --mode 1920x1080 --right-of eDP-1
}

if [ "$LEN" -gt 1 ]; then
  dual_monitors
fi
