***REMOVED***!/bin/sh

IFS=$'\n'
lf_trash() {
  clear; tput bold; tput setaf 4;
  printf "\n%s" "$@"

  ***REMOVED*** Set color to red
  tput setaf 1;
  printf "\n\tdelete?[y/N] "
  read ans

  [ "$ans" != "y" ] && exit
  for file in "$@"; do
    trash-put -- "$file"
  done
}

lf_trash "$@"
clear
